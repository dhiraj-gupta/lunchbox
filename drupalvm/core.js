// core DVM class
(function(context) {
  "use strict";

  // add DVM class to window scope
  if (!context.DVM) {
    context.DVM = (function(context) {
      // private


      // public
      return {

      };
    })(context);
  }
})(window);